# ⚠️ 该文档已废弃 (DEPRECATED)

请使用 **`docs/CRON_JOBS.md`** —— 这是 Hermes cron 任务的**单一事实来源**。

旧文档描述的是不存在的 "Hermes YAML skill 文件" 配置方式。实际系统使用 **Hermes cron 任务**
（`/cron list` 查看，4 个任务），全部规范见 `docs/CRON_JOBS.md`。

本文件保留仅为历史参考。**不要再按这里的内容配置。**
